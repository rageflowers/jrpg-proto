from __future__ import annotations
import pygame


def fade_in(surface: pygame.Surface, duration_ms: int = 500) -> None:
    """Placeholder: a blocking fade-in effect.

    In a real-time game loop, this will become an incremental effect.
    For now we leave it as a stub to be integrated later.
    """
    # We'll implement this in a future phase.
    pass


def fade_out(surface: pygame.Surface, duration_ms: int = 500) -> None:
    """Placeholder: a blocking fade-out effect."""
    pass
